import * as messageJson from './messages.json';

/* This is cnfig file where all the config 
    changes related to users are going to be added 
    All messages are going to be added in messages.json
*/
export default {
    MESSAGES: messageJson,
};